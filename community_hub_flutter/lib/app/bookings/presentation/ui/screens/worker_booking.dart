import 'package:community_hub/lib.dart';

@RoutePage(name: 'workerBooking')
class WorkerBookingScreen extends StatelessWidget {
  const WorkerBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
