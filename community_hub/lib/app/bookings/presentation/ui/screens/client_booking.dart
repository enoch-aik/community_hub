import 'package:community_hub/lib.dart';

@RoutePage(name: 'clientBooking')
class ClientBookingScreen extends StatelessWidget {
  const ClientBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
